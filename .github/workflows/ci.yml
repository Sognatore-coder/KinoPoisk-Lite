name: CI

on:
  push:
    branches: [ master ]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Syntax Check
      run: find js/ -name "*.js" -exec node -c {} \;